import 'package:flutter/material.dart';

///Creates loading indicator to be displayed while information
///is being recieved.
Widget buildLoadingIndicator() =>
    const Center(child: CircularProgressIndicator());
